name: dongdong-tarot
service:
  type: web
  runtime: python
  runtime_version: "3.12"
  build_command: pip install -r requirements-light.txt
  start_command: uvicorn app.main:app --host 0.0.0.0 --port $PORT
  workdir: backend
  instance_type: free
  env:
    DEEPSEEK_API_KEY: "${DEEPSEEK_API_KEY}"
